# We're testing the aws_auth feature of the python plugin here.
plugin_packages:
    - ../../..[aws_auth]
plugins:
    - csspin_python.python
python:
    use: python
    aws_auth:
        enabled: True
        static_oidc: True
